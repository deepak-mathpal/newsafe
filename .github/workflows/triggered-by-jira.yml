name: on-demand-test

on:
  repository_dispatch:
    types:
      - postman-event


jobs:
  process-jira:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Read Payload from Repository Dispatch
        run: |
          echo "Jira ID:D"
          echo "Summary: "
          echo "Assignee: "
